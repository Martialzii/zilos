[project]
name = "zilos"
version = "0.3.0"
description = "Zilos CLI for micro activity tracking, project reporting, and advanced stats."
authors = ["Cyrus <cyssifa@gmail.com>"]
readme = "README.md"
requires-python = ">=3.9"
license = {text = "MIT"}

[project.scripts]
zilos = "zilos:main"
